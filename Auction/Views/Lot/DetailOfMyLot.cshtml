@model Auction.Models.Lot
<section class="d-flex flex-row bd-highlight" style="justify-content: space-evenly">
    <div>
        <image src="/@Model.PictureUrl" class="img-thumbnail " style="width: 400px; height: 400px"/>
        <p>Name : @Model.Name</p>
        <p>Price : @Model.Price</p>
    </div>
</section>